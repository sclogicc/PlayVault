# PlayVault 开发模式（热更新）

当需要查看正在修改的界面、样式或功能时，双击项目根目录的 **`启动开发模式.cmd`**。第一次启动会打开 PlayVault 和一个命令行窗口；**命令行窗口必须保持打开**，因为它负责监听源代码变化。

渲染层的 React 页面与 CSS 保存后会自动刷新，无须执行 `npm run build`。主进程或预加载脚本发生变化时，开发工具会重新启动 PlayVault，窗口可能短暂关闭后重新出现。这是正常行为。

| 场景 | 应使用的方式 |
|---|---|
| 正在验证界面或功能修改 | 双击 `启动开发模式.cmd`，随后保持命令行窗口运行。 |
| 日常游玩与稳定使用 | 使用原有 PlayVault 快捷方式。 |
| 拉取新代码后的稳定版本 | 关闭 PlayVault 后执行 `npm run build`，再用原有快捷方式启动。 |

开发完成后，关闭 PlayVault 窗口并在命令行中按 `Ctrl + C` 即可结束开发模式。若下次启动前安装了新依赖，先执行一次 `npm ci`；普通界面和功能更新不需要重复安装依赖。
